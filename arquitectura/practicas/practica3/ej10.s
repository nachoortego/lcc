data
a: .long 1, 2, 3, 4
g: .quad 0x1122334455667788
msg: .asciz "Imprime %c\n"
.text
4
.global main
main:
  subq $8, %rsp               # rsp =..........................
  movq $g, %rax               # rax =..........................
  movl g+4, %eax              # rax =..........................
  movq $3, %rbx               # rbx =..........................
  movb a(,%rbx,4), %al        # rax =..........................
  leaq msg, %rdi              # rdi =..........................
  movb (%rdi, %rbx, 2), %sil  # sil =..........................
  xorl %eax, %eax             # rax =..........................
  call printf                 # rax =..........................
  addq $8, %rsp               # rsp =..........................
  ret